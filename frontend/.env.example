# Frontend API endpoint
# Dev default is http://localhost:8000/api and production default is /api.
# Set this when frontend and backend are hosted on different domains.
VITE_API_BASE_URL=https://your-api-domain.com/api
